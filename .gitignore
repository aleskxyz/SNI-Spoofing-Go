# Binaries (see Makefile)
/sni-spoofing
/sni-spoofing.exe
/dist/

# Go build / test
*.test
*.out
coverage.out
coverage.html
/vendor/

# WinDivert download cache (make windivert)
.cache/

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local overrides (optional: uncomment if you keep secrets out of config.json)
# config.local.json

# WinDivert runtime files often copied next to the Windows binary (not in repo)
WinDivert.dll
WinDivert32.sys
WinDivert64.sys

# Wails GUI build outputs and generated bindings
/gui/build/bin/
/gui/sni-spoofing-gui*.exe
/gui/frontend/dist/*
!/gui/frontend/dist/.gitkeep
/gui/frontend/node_modules/
/gui/frontend/wailsjs/
# wails build cache stamp — regenerated whenever package.json changes
/gui/frontend/package.json.md5
