# Frontend

Svelte 5 + Vite 7 + TypeScript app for the SNI-Spoofing GUI. Built and embedded into the Go binary by `wails build`; see [`../README.md`](../README.md) for the full build/dev workflow.

You don't normally run `npm` commands here directly — `wails build` and `wails dev` do that for you.
